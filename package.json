{
  "name": "fixice-static-server",
  "version": "1.0.0",
  "description": "Static site with live reload for fixice",
  "scripts": {
    "start": "node server.js",
    "dev": "live-server dist --port=8080 --no-browser"
  },
  "dependencies": {
    "express": "^4.18.2",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "live-server": "^1.2.2",
    "playwright": "^1.58.2"
  }
}
